# Eclipse で競合解消のために ShiftService.java を最新版へ合わせる手順

以下の手順は、GitHub 上の最新 `ShiftService.java` をローカル環境へ反映させる際に Eclipse (EGit) の GUI を用いて競合を解消するための具体的な流れです。

## 前提
- Eclipse に EGit がインストールされていること。
- 対象ブランチが `main` と仮定します。別ブランチの場合は読み替えてください。
- ローカルに未コミットの変更が残っている場合は、必要に応じてスタッシュまたはバックアップを取ってください。

## 手順

1. **リモートの最新情報を取得する（Fetch）**
   1. Eclipse のメニューから **[ウィンドウ] > [パースペクティブを開く] > [その他]** を選択し、**Git** パースペクティブに切り替えます。
   2. Git リポジトリー・ビューで対象リポジトリを右クリックし、**[リモート] > [フェッチ]** を選択します。
   3. ダイアログで `origin` を選択し、**[完了]** をクリックしてフェッチを実行します。

2. **ローカルの `main` ブランチをリモートに合わせてリセットする**
   1. Git リポジトリー・ビューで `Branches > Local` 配下の `main` を右クリックし、**[ブランチをチェックアウト]** で `main` に切り替えます。
   2. 再度 `main` を右クリックし、**[リセット]** を選択します。
   3. リセットのダイアログで次を設定します。
      - **リセット先参照**: `origin/main`
      - **リセットの種類**: `ハード`
   4. **[OK]** をクリックすると、ローカルの `main` が `origin/main` と完全に一致します。

3. **作業ブランチに `main` の内容を取り込む（マージ）**
   1. 作業中のブランチへ切り替えるため、`Branches > Local` で対象ブランチを右クリックし **[ブランチをチェックアウト]** を実行します。
   2. 同じブランチを右クリックし、**[マージ]** を選択します。
   3. マージ対象として `origin/main`（または `main`）を指定し、**[次へ]** → **[完了]** をクリックします。
   4. 競合が発生した場合は、Eclipse が競合解決ビューを開きますが、この段階で `ShiftService.java` は `origin/main` と同一内容のため、多くの場合自動で解決されます。

4. **競合が残った場合の確認と解決**
   1. **[Git ステージング]** ビューで `ShiftService.java` が `Conflicts` セクションにないか確認します。
   2. 競合がある場合は、ファイルをダブルクリックして **[比較エディター]** を開き、`HEAD`（ローカル）・`MERGE_HEAD`（マージ対象）・`共同祖先` の差分を確認します。
   3. `ShiftService.java` を GitHub の最新版と一致するように編集し、保存後 **[マージを完了]** をクリックします。

5. **変更をステージしてコミットする**
   1. 競合解消後の `ShiftService.java` が **[Git ステージング]** ビューの `未ステージ` セクションに表示されます。
   2. ファイルを選択して **[追加]** ボタン（`+` アイコン）でステージします。
   3. コミットメッセージ欄に例として `Fix ShiftService conflict by aligning with remote` などと入力し、**[コミット]** をクリックします。

6. **リモートへプッシュする**
   1. Git リポジトリー・ビューで作業ブランチを右クリックし、**[プッシュ]** を選択します。
   2. リモートとして `origin` を選び、ブランチのマッピングを確認して **[次へ]** → **[完了]** をクリックします。

## 補足
- `git stash` に相当する GUI 操作は、作業ツリーの変更をパッチとしてエクスポートするか、別ブランチに一時コミットする方法で代替できます。
- ターミナル操作が可能な場合は、Eclipse のメニュー **[ウィンドウ] > [表示] > [その他] > [一般] > [端末]** からターミナルビューを開き、前述の Git コマンドを直接入力することもできます。

