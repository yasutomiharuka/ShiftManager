pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://maven-central.storage-download.googleapis.com/maven2' }
        mavenCentral()
    }
}

rootProject.name = 'ShiftManager'
